Rework on file structure
E.g. \App\Blueprint -> frame/structure of the app such as utilities, view, APIs wrapper etc.
E.g. \App\Swiften -> for Cli features.

Integrate Queues (using SQLite or find alternatives)

Migrate Cron to SQLite (to allow for dashboard management of cron tasks)

Migrate filebased tokens etc to SQL based. E.g. google recaptcha key/token.

Integrate Views.php into Router.php inside of Blueprint\Web

Fix dependencies